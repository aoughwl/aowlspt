# MEASURED live-inspector output. Every OUTPUT block below is VERBATIM host
# text captured from a real run on this machine on 2026-09-01/02 -- pointers,
# counts, frame numbers and all. Nothing here is hand-written prose.
#
# WHY THIS EXISTS: three tools parse this prose (acceptance.py,
# nativetabs_check.py, uihooks_check.py) and each had grown its own private
# replay. Two of them passed VACUOUSLY on their own command echo or on a
# fragment of the batch header before being fixed. One corpus, loaded by
# tools/inspectfixtures.py, is how that stays fixed: every parser test runs
# against text the host really emitted, including its refusals.
#
# DO NOT EDIT AN OUTPUT BLOCK BY HAND. If the host's wording changes, re-harvest
# from a real capture and say where it came from in SOURCE. A fixture someone
# tidied is no longer evidence.
#
# Entry format:  === ENTRY <id> / --- SOURCE / --- SHAPE / --- COMMAND /
#                --- OUTPUT <verbatim ...> / --- END

=== ENTRY find-exhaustive-hit
--- SOURCE scratchpad boot3.out, batch 12
--- SHAPE one HIT, searched EXHAUSTIVELY, $f1 bound
--- COMMAND find TaskBar 0x000001b0e84eb060 40000
--- OUTPUT
aowlspt live inspector -- batch 12, 2 command(s), on Unity thread 17484
> find TaskBar 0x000001b0e84eb060 40000
  searching from 0x000001b0e84eb060 name="Preloader UI" for name containing "TaskBar" (case-insensitive; budget 40000 nodes, 12ms per frame, continuing across frames automatically)
    HIT 0x000001b0e84e4200 name="TaskBar"  parent="Content"   ($f1)
  visited 4073 node(s) over 32 frame(s), 1 match(es), frontier 0 node(s)
  -- the subtree was searched EXHAUSTIVELY. Every match is listed above.
  matches are bound to $f1..$f1 for the rest of this batch
> echo aowl-batch-1788297642202-23
  aowl-batch-1788297642202-23
aowlspt live inspector -- batch 12 complete, 9 line(s)
--- END

=== ENTRY find-stopped-early
--- SOURCE scratchpad boot3.out, batch 11
--- SHAPE 2 hits but STOPPED EARLY on the node budget -- hits are real, absence is NOT proven
--- COMMAND find "Preloader UI"
--- OUTPUT
aowlspt live inspector -- batch 11, 2 command(s), on Unity thread 17484
> find "Preloader UI"
  searching ALL 14 scene root(s) for name containing "Preloader UI" (case-insensitive; budget 20000 nodes, 12ms per frame, continuing across frames automatically)
    HIT 0x000001b0e84eb060 name="Preloader UI"   ($f1)
    HIT 0x000001b0e84e4a60 name="Preloader UI"  parent="Preloader UI"   ($f2)
  visited 20000 node(s) over 157 frame(s), 2 match(es), frontier 11179 node(s)
  -- STOPPED EARLY on the NODE BUDGET. This is NOT proof the subject is absent. Continue with `find more` (it RESUMES from the frontier above and does not re-walk what it already rejected), or re-run with a bigger budget, or search from a narrower root.
  matches are bound to $f1..$f2 for the rest of this batch
> echo aowl-batch-1788297627892-22
  aowl-batch-1788297627892-22
aowlspt live inspector -- batch 11 complete, 10 line(s)
--- END

=== ENTRY find-nothing-examined
--- SOURCE scratchpad bootU.out, batch 74
--- SHAPE explicit ROOT did not resolve: `Nothing was searched.` ZERO nodes visited -- neither EXHAUSTIVE nor STOPPED EARLY
--- COMMAND find Toggle in ClonedSpawner all
--- OUTPUT
aowlspt live inspector -- batch 74, 2 command(s), on Unity thread 1192
> find Toggle in ClonedSpawner all
  ! no object named like "ClonedSpawner" was found to use as a root. Nothing was searched.
> echo aowl-batch-1788322246647-9
  aowl-batch-1788322246647-9
aowlspt live inspector -- batch 74 complete, 5 line(s)
--- END

=== ENTRY find-genuinely-not-present
--- SOURCE scratchpad bootH.out, batch 1
--- SHAPE EXHAUSTIVE with 0 hits over 1 node: the blind `genuinely NOT PRESENT` verdict, plus the trailing-token BUDGET-not-ROOT note and `find more` with no capped search to continue
--- COMMAND find MenuScreen 80000
--- OUTPUT
aowlspt live inspector -- batch 1, 5 command(s), on Unity thread 20272
> find MenuScreen 80000
  NOTE: "80000" does not read back as a live pointer, so it is being treated as BUDGET, not ROOT. Searching every scene root (as if no root were given). If you meant a root, pass a pointer that is actually live.
  searching ALL 1 scene root(s) for name containing "MenuScreen" (case-insensitive; budget 80000 nodes, 12ms per frame, continuing across frames automatically)
  visited 1 node(s) over 1 frame(s), 0 match(es), frontier 0 node(s)
  -- the subtree was searched EXHAUSTIVELY. That name is genuinely NOT PRESENT under this root.
> find more 80000
  ! there is no capped search to continue. Run `find NAME ...` first.
> find more 80000
  ! there is no capped search to continue. Run `find NAME ...` first.
> path $f1
  ! no such anchor or variable: $f1
> echo aowl-batch-1788308491064-66
  aowl-batch-1788308491064-66
aowlspt live inspector -- batch 1 complete, 14 line(s)
--- END

=== ENTRY find-toggles-strip-with-clones
--- SOURCE live client, 2026-09-02, Settings open, mods tab built; captured by tools/ichannel.py run_batch (the `echo <sentinel>` line it appends is stripped by run_batch itself, which is why no `> echo` echo appears), batch 62
--- SHAPE EXHAUSTIVE `find`, 18 hits: the `Toggles` strip under SettingsScreen, its 5 stock spawners, 8 `GameToggleSpawner(Clone)` mod-tab clones, and the POSTFX subtab strips
--- COMMAND find Toggles $settings 40000
--- OUTPUT
aowlspt live inspector -- batch 62, 2 command(s), on Unity thread 16676
> find Toggles $settings 40000
  searching from 0x000002024b079ec0 name="SettingsScreen" for name containing "Toggles" (case-insensitive; budget 40000 nodes, 12ms per frame, continuing across frames automatically)
    HIT 0x000002024b079ee0 name="Toggles"  parent="SettingsScreen"   ($f1)
    HIT 0x000002024b079e80 name="GameToggleSpawner"  parent="Toggles"   ($f2)
    HIT 0x000002024b079e00 name="GraphicsToggleSpawner"  parent="Toggles"   ($f3)
    HIT 0x000002024b079da0 name="PostFxToggleSpawner"  parent="Toggles"   ($f4)
    HIT 0x000002024b079d20 name="SoundToggleSpawner"  parent="Toggles"   ($f5)
    HIT 0x000002024b079cc0 name="ControlsToggleSpawner"  parent="Toggles"   ($f6)
    HIT 0x000002030962f640 name="GameToggleSpawner(Clone)"  parent="Toggles"   ($f7)
    HIT 0x00000203097aeae0 name="GameToggleSpawner(Clone)"  parent="Toggles"   ($f8)
    HIT 0x00000203090dcb80 name="GameToggleSpawner(Clone)"  parent="Toggles"   ($f9)
    HIT 0x00000203091dfaa0 name="GameToggleSpawner(Clone)"  parent="Toggles"   ($f10)
    HIT 0x00000203092f49a0 name="GameToggleSpawner(Clone)"  parent="Toggles"   ($f11)
    HIT 0x00000203093df1e0 name="GameToggleSpawner(Clone)"  parent="Toggles"   ($f12)
    HIT 0x0000020308cd9c00 name="GameToggleSpawner(Clone)"  parent="Toggles"   ($f13)
    HIT 0x0000020308da9440 name="GameToggleSpawner(Clone)"  parent="Toggles"   ($f14)
    HIT 0x0000020309629dc0 name="Toggles(Clone)"  parent="Graphics Settings"   ($f15)
    HIT 0x00000203096291c0 name="Toggles(Clone)"  parent="PostFX Settings"   ($f16)
    HIT 0x000002024b0719c0 name="Toggles"  parent="Control Settings"   ($f17)
    HIT 0x000002024a9be960 name="TogglesSection"  parent="Panel"   ($f18)
  visited 7735 node(s) over 61 frame(s), 18 match(es), frontier 0 node(s)
  -- the subtree was searched EXHAUSTIVELY. Every match is listed above.
  matches are bound to $f1..$f18 for the rest of this batch
aowlspt live inspector -- batch 62 complete, 26 line(s)
--- END
