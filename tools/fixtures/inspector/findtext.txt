# MEASURED live-inspector output. Every OUTPUT block below is VERBATIM host
# text captured from a real run on this machine on 2026-09-01/02 -- pointers,
# counts, frame numbers and all. Nothing here is hand-written prose.
#
# WHY THIS EXISTS: three tools parse this prose (acceptance.py,
# nativetabs_check.py, uihooks_check.py) and each had grown its own private
# replay. Two of them passed VACUOUSLY on their own command echo or on a
# fragment of the batch header before being fixed. One corpus, loaded by
# tools/inspectfixtures.py, is how that stays fixed: every parser test runs
# against text the host really emitted, including its refusals.
#
# DO NOT EDIT AN OUTPUT BLOCK BY HAND. If the host's wording changes, re-harvest
# from a real capture and say where it came from in SOURCE. A fixture someone
# tidied is no longer evidence.
#
# Entry format:  === ENTRY <id> / --- SOURCE / --- SHAPE / --- COMMAND /
#                --- OUTPUT <verbatim ...> / --- END

=== ENTRY findtext-hit-inactive
--- SOURCE scratchpad boot5.out, batch 5
--- SHAPE a HIT carrying text= and activeInHierarchy=FALSE (unpressable), scope ALL, plus the budget-clamp note
--- COMMAND findtext loading $preloader 120000 all
--- OUTPUT
aowlspt live inspector -- batch 5, 2 command(s), on Unity thread 13076
> findtext loading $preloader 120000 all
  NOTE: requested budget 120000 exceeds `findtext`'s ceiling of 60000 nodes; clamped to 60000. The real backstop is the 12ms per-frame time slice, not the node count -- raising this ceiling is a deliberate code change, not a per-call override.
  searching from 0x0000013b7b79bee0 name="Preloader UI" for TMP text containing "loading" (case-insensitive; budget 60000 nodes, 12ms per frame, continuing across frames automatically; scope: ALL nodes, active and inactive)
    HIT 0x0000013be6206440  name="Text"  parent="LoadingSpinner"  text="Loading..."  activeInHierarchy=FALSE (unpressable)   ($f1)
  visited 4073 node(s) over 23 frame(s), 1 match(es), frontier 0 node(s)
  -- the subtree was searched EXHAUSTIVELY. Every match is listed above.
  matches are bound to $f1..$f1 for the rest of this batch
  scope: ALL nodes, active and inactive -- `all` was passed, so some hits above may be unpressable; check activeInHierarchy on each before pressing
> echo aowl-batch-1788300430504-0
  aowl-batch-1788300430504-0
aowlspt live inspector -- batch 5 complete, 11 line(s)
--- END

=== ENTRY findtext-stopped-early-zero
--- SOURCE scratchpad boot3.out, batch 19
--- SHAPE 0 matches, STOPPED EARLY, scope ACTIVE only -- the shape that must never be read as absence
--- COMMAND findtext GRAPHICS
--- OUTPUT
aowlspt live inspector -- batch 19, 2 command(s), on Unity thread 17484
> findtext GRAPHICS
  searching ALL 16 scene root(s) for TMP text containing "GRAPHICS" (case-insensitive; budget 20000 nodes, 12ms per frame, continuing across frames automatically; scope: ACTIVE nodes only -- pass `all` to include inactive)
  visited 15552 node(s) over 240 frame(s), 0 match(es), frontier 15103 node(s)
  -- STOPPED EARLY on the NODE BUDGET. This is NOT proof the subject is absent. Continue with `findtext more` (it RESUMES from the frontier above and does not re-walk what it already rejected), or re-run with a bigger budget, or search from a narrower root.
  scope: ACTIVE nodes only (activeInHierarchy)
> echo aowl-batch-1788297676076-30
  aowl-batch-1788297676076-30
aowlspt live inspector -- batch 19 complete, 8 line(s)
--- END

=== ENTRY findtext-clones-donor-caption
--- SOURCE live client, 2026-09-02, Settings open, mods tab built; captured by tools/ichannel.py run_batch (the `echo <sentinel>` line it appends is stripped by run_batch itself, which is why no `> echo` echo appears), batch 56
--- SHAPE a MULTI-WORD needle passed QUOTED, 24 hits, HIT_CAP (the match cap is 24), hits split between the stock row `Settings Drop Down(Clone)` and OUR row clones `Settings Drop Down(Clone)(Clone)` -- and every HIT line carries `text=` and `activeInHierarchy=`, which channel.py's _HIT_RE does not match at all
--- COMMAND findtext "Interface language" $settings 40000 all
--- OUTPUT
aowlspt live inspector -- batch 56, 2 command(s), on Unity thread 16676
> findtext "Interface language" $settings 40000 all
  searching from 0x000002024b079ec0 name="SettingsScreen" for TMP text containing "Interface language" (case-insensitive; budget 40000 nodes, 12ms per frame, continuing across frames automatically; scope: ALL nodes, active and inactive)
    HIT 0x00000203094ddf60  name="Text"  parent="Settings Drop Down(Clone)"  text="Interface language"  activeInHierarchy=true   ($f1)
    HIT 0x00000203095d3e00  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f2)
    HIT 0x00000203095d36c0  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f3)
    HIT 0x00000203095daf60  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f4)
    HIT 0x00000203095da7e0  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f5)
    HIT 0x00000203095da140  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f6)
    HIT 0x00000203095f3aa0  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f7)
    HIT 0x00000203095f3400  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f8)
    HIT 0x00000203095fcb60  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f9)
    HIT 0x00000203095fc4c0  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f10)
    HIT 0x0000020309604d60  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f11)
    HIT 0x00000203096046c0  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f12)
    HIT 0x000002030960bfe0  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f13)
    HIT 0x000002030960b920  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f14)
    HIT 0x000002030960b280  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f15)
    HIT 0x00000203096bcaa0  name="Text"  parent="Settings Drop Down(Clone)"  text="Interface language"  activeInHierarchy=true   ($f16)
    HIT 0x00000203096c31e0  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f17)
    HIT 0x00000203096c3100  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f18)
    HIT 0x00000203096c3020  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f19)
    HIT 0x00000203096caf40  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f20)
    HIT 0x00000203096cae60  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f21)
    HIT 0x00000203096cad80  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f22)
    HIT 0x00000203096caca0  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f23)
    HIT 0x00000203096cabc0  name="Text"  parent="Settings Drop Down(Clone)(Clone)"  text="Interface language"  activeInHierarchy=true   ($f24)
  visited 1297 node(s) over 20 frame(s), 24 match(es), frontier 1352 node(s)
  -- stopped at the match cap (24). There may be more; `findtext more` continues from the frontier above.
  matches are bound to $f1..$f24 for the rest of this batch
  predicate control: 225 node(s) carried a TextMeshProUGUI (of 1297 visited) -- the predicate demonstrably CAN answer yes on this subtree.
  scope: ALL nodes, active and inactive -- `all` was passed, so some hits above may be unpressable; check activeInHierarchy on each before pressing
aowlspt live inspector -- batch 56 complete, 34 line(s)
--- END

=== ENTRY findtext-placeholder-inactive-only
--- SOURCE live client, 2026-09-02, Settings open, mods tab built; captured by tools/ichannel.py run_batch (the `echo <sentinel>` line it appends is stripped by run_batch itself, which is why no `> echo` echo appears), batch 57
--- SHAPE EXHAUSTIVE walk, 12 hits, ALL activeInHierarchy=FALSE -- the donor prefabs still carry the placeholder but nothing renders it; also carries the in-band `predicate control:` line that makes a separate control walk unnecessary
--- COMMAND findtext "SOME TEXT" $settings 40000 all
--- OUTPUT
aowlspt live inspector -- batch 57, 2 command(s), on Unity thread 16676
> findtext "SOME TEXT" $settings 40000 all
  searching from 0x000002024b079ec0 name="SettingsScreen" for TMP text containing "SOME TEXT" (case-insensitive; budget 40000 nodes, 12ms per frame, continuing across frames automatically; scope: ALL nodes, active and inactive)
    HIT 0x0000020309629c00  name="SizeLabel"  parent="AnimatedToggle"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f1)
    HIT 0x0000020309629a20  name="SizeLabel"  parent="AnimatedToggle"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f2)
    HIT 0x000002030962ffe0  name="SizeLabel"  parent="AnimatedToggle"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f3)
    HIT 0x000002030962fde0  name="SizeLabel"  parent="AnimatedToggle"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f4)
    HIT 0x0000020309629660  name="SizeLabel"  parent="AnimatedToggle"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f5)
    HIT 0x0000020308e89e80  name="SizeLabel"  parent="AnimatedToggle"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f6)
    HIT 0x0000020309629ba0  name="Label"  parent="SizeLabel"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f7)
    HIT 0x00000203096299c0  name="Label"  parent="SizeLabel"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f8)
    HIT 0x000002030962ff60  name="Label"  parent="SizeLabel"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f9)
    HIT 0x000002030962fd80  name="Label"  parent="SizeLabel"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f10)
    HIT 0x0000020309629640  name="Label"  parent="SizeLabel"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f11)
    HIT 0x0000020308e89a20  name="Label"  parent="SizeLabel"  text="SOME TEXT"  activeInHierarchy=FALSE (unpressable)   ($f12)
  visited 7735 node(s) over 62 frame(s), 12 match(es), frontier 0 node(s)
  -- the subtree was searched EXHAUSTIVELY. Every match is listed above.
  matches are bound to $f1..$f12 for the rest of this batch
  predicate control: 1127 node(s) carried a TextMeshProUGUI (of 7735 visited) -- the predicate demonstrably CAN answer yes on this subtree.
  scope: ALL nodes, active and inactive -- `all` was passed, so some hits above may be unpressable; check activeInHierarchy on each before pressing
aowlspt live inspector -- batch 57 complete, 22 line(s)
--- END

=== ENTRY findtext-unquoted-multiword-refusal
--- SOURCE synthetic-from-spec, 2026-09-02. NOT a capture. Written from the refusal text added to iFindRefuseWordyRoot in host/Aowlspt.Host.Il2Cpp/inspect.nim after two agents independently hit the shape live (`findtext Interface language $settings 40000 all` -> `! not an address: language`, 0 hits, no completeness line). REPLACE THIS BLOCK with a real capture the first time the rebuilt host emits it; until then it is a spec, not evidence.
--- SHAPE an UNQUOTED multi-word needle: the second word lands in the ROOT slot, the whole command is REFUSED, and the completeness line reads NOTHING WAS EXAMINED so a hit-counting parser cannot read it as a clean 0-hit miss
--- COMMAND findtext Interface language $settings 40000 all
--- OUTPUT
aowlspt live inspector -- batch 7, 2 command(s), on Unity thread 16676
> findtext Interface language $settings 40000 all
  ! findtext: needle is ONE token; "language" was read as the ROOT and is not one. Quote a multi-word needle: findtext "Interface language" $settings 40000
  visited 0 node(s) over 0 frame(s), 0 match(es), frontier 0 node(s)
  -- NOTHING WAS EXAMINED (0 valid nodes visited). This is NOT evidence the search subject is absent -- it means `findtext` refused before it looked. Nothing was searched.
> echo aowl-batch-1788300430504-7
  aowl-batch-1788300430504-7
aowlspt live inspector -- batch 7 complete, 7 line(s)
--- END

=== ENTRY findtext-inactive-ancestor-skipped
--- SOURCE synthetic-from-spec, NOT measured -- composed from inspect.nim's own
--- SOURCE format strings for the fix to the measured 2026-09-02 defect where
--- SOURCE `findtext Savant 200000` (default ACTIVE-only scope) returned a HIT on a
--- SOURCE CharacterSlotView_pvp under an INACTIVE CharacterSelectionScreen while
--- SOURCE `assert-active` on the same card said activeInHierarchy=false. Re-harvest
--- SOURCE and re-mark as measured the next time this shape is captured live.
--- SHAPE default ACTIVE scope: the inactive-ancestor hit is SKIPPED and the skip is counted and named, so 0 matches here is NOT absence
--- COMMAND findtext Savant 200000
--- OUTPUT
aowlspt live inspector -- batch 7, 2 command(s), on Unity thread 13076
> findtext Savant 200000
  NOTE: "200000" does not read back as a live pointer, so it is being treated as BUDGET, not ROOT. Searching every scene root (as if no root were given). If you meant a root, pass a pointer that is actually live.
  NOTE: requested budget 200000 exceeds `findtext`'s ceiling of 60000 nodes; clamped to 60000. The real backstop is the 12ms per-frame time slice, not the node count -- raising this ceiling is a deliberate code change, not a per-call override.
  searching ALL 16 scene root(s) for TMP text containing "Savant" (case-insensitive; budget 60000 nodes, 12ms per frame, continuing across frames automatically; scope: ACTIVE nodes only -- pass `all` to include inactive)
  visited 5211 node(s) over 29 frame(s), 0 match(es), frontier 0 node(s)
  -- the subtree was searched EXHAUSTIVELY. Every match is listed above.
  predicate control: 388 node(s) carried a TextMeshProUGUI (of 5211 visited) -- the predicate demonstrably CAN answer yes on this subtree.
  scope: ACTIVE nodes only (activeInHierarchy, cross-checked against the parent chain -- the same predicate `assert-active` uses); 1 inactive hit(s) skipped -- re-run as `findtext ... all` to include them
> echo aowl-batch-1788300430504-0
  aowl-batch-1788300430504-0
aowlspt live inspector -- batch 7 complete, 10 line(s)
--- END

=== ENTRY findtext-active-unknown-disagreement
--- SOURCE synthetic-from-spec, NOT measured -- see findtext-inactive-ancestor-skipped.
--- SOURCE This is the CROSS-CHECK branch: GameObject::get_activeInHierarchy and the
--- SOURCE independent parent-chain derivation disagreed, which is what a shared
--- SOURCE empty-body stub at that RVA looks like from the outside.
--- SHAPE a HIT tagged activeInHierarchy=UNKNOWN plus the INCONCLUSIVE line -- must NOT be parsed as pressable, and must NOT be parsed as a clean hit
--- COMMAND findtext Savant 200000
--- OUTPUT
aowlspt live inspector -- batch 8, 2 command(s), on Unity thread 13076
> findtext Savant 200000
  NOTE: "200000" does not read back as a live pointer, so it is being treated as BUDGET, not ROOT. Searching every scene root (as if no root were given). If you meant a root, pass a pointer that is actually live.
  searching ALL 16 scene root(s) for TMP text containing "Savant" (case-insensitive; budget 60000 nodes, 12ms per frame, continuing across frames automatically; scope: ACTIVE nodes only -- pass `all` to include inactive)
    HIT 0x0000013be6206440  name="Nickname"  parent="CharacterSlotView_pvp"  text="Savant"  activeInHierarchy=UNKNOWN (NOT verified pressable)   ($f1)
  visited 5211 node(s) over 29 frame(s), 1 match(es), frontier 0 node(s)
  -- the subtree was searched EXHAUSTIVELY. Every match is listed above.
  matches are bound to $f1..$f1 for the rest of this batch
  predicate control: 388 node(s) carried a TextMeshProUGUI (of 5211 visited) -- the predicate demonstrably CAN answer yes on this subtree.
  scope: ACTIVE nodes only (activeInHierarchy, cross-checked against the parent chain -- the same predicate `assert-active` uses)
  ! INCONCLUSIVE for 1 hit(s): their activeInHierarchy could not be established, so they are listed but NOT claimed pressable. Confirm each with `assert-active $fN` before pressing it.
  ! active predicate: activeInHierarchy CROSS-CHECK DISAGREEMENT on 0x0000013be6206440: GameObject::get_activeInHierarchy said true, but the parent chain (get_activeSelf at every level) said false -- inactive ancestor "CharacterSelectionScreen". One of the two is wrong, so this node's pressability is INCONCLUSIVE and it is NOT reported as pressable.
> echo aowl-batch-1788300430504-0
  aowl-batch-1788300430504-0
aowlspt live inspector -- batch 8 complete, 13 line(s)
--- END

=== ENTRY findtext-in-root-substring-refused
--- SOURCE synthetic-from-spec, NOT measured -- composed from inspect.nim's format
--- SOURCE strings for the measured 2026-09-02 defect where `find X in Menu UI` read
--- SOURCE ONE token as the root name, resolved "Menu" by SUBSTRING to "Context Menu
--- SOURCE Area", searched that, and answered "genuinely NOT PRESENT under this root".
--- SHAPE THE NEGATIVE CONTROL: a substring-only root match is REFUSED and NOTHING WAS EXAMINED -- a parser must classify this as inconclusive, never as absence
--- COMMAND findtext Savant in Menu
--- OUTPUT
aowlspt live inspector -- batch 9, 2 command(s), on Unity thread 13076
> findtext Savant in Menu
  ! findtext REFUSED: no object is named exactly "Menu". The closest match by substring is "Context Menu Area" at 0x0000013b7c114a80, and searching THAT and calling the result an absence is the confidently-wrong answer this refusal exists to prevent. If you meant it, pass its pointer, or spell the name in full (quote it: findtext NAME in "Context Menu Area"). Nothing was searched.
  visited 0 node(s) over 0 frame(s), 0 match(es), frontier 0 node(s)
  -- NOTHING WAS EXAMINED (0 valid nodes visited). This is NOT evidence the search subject is absent -- it means `findtext` refused before it looked. Nothing was searched.
> echo aowl-batch-1788300430504-0
  aowl-batch-1788300430504-0
aowlspt live inspector -- batch 9 complete, 7 line(s)
--- END

=== ENTRY findtext-in-root-exact-scene-root
--- SOURCE synthetic-from-spec, NOT measured -- see findtext-in-root-substring-refused.
--- SOURCE The quoted form used to find no root at all, because "Login UI" is a SCENE
--- SOURCE ROOT and the old resolver only walked the tree $preloader belongs to.
--- SHAPE a quoted multi-word root resolved by EXACT name among the scene roots, then searched normally
--- COMMAND findtext Savant in "Login UI"
--- OUTPUT
aowlspt live inspector -- batch 10, 2 command(s), on Unity thread 13076
> findtext Savant in "Login UI"
  root "Login UI" resolved by EXACT name among the 16 scene root(s) to 0x0000013b7b6f21c0.
  searching from 0x0000013b7b6f21c0 name="Login UI" for TMP text containing "Savant" (case-insensitive; budget 20000 nodes, 12ms per frame, continuing across frames automatically; scope: ACTIVE nodes only -- pass `all` to include inactive)
  visited 812 node(s) over 4 frame(s), 0 match(es), frontier 0 node(s)
  -- the subtree was searched EXHAUSTIVELY. Every match is listed above.
  predicate control: 96 node(s) carried a TextMeshProUGUI (of 812 visited) -- the predicate demonstrably CAN answer yes on this subtree.
  scope: ACTIVE nodes only (activeInHierarchy, cross-checked against the parent chain -- the same predicate `assert-active` uses); 1 inactive hit(s) skipped -- re-run as `findtext ... all` to include them
> echo aowl-batch-1788300430504-0
  aowl-batch-1788300430504-0
aowlspt live inspector -- batch 10 complete, 9 line(s)
--- END
